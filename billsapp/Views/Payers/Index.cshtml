@model IEnumerable<billsapp.Models.payer>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.payer_firstname)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.payer_lastname)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.payer_firstname)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.payer_lastname)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.payer_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.payer_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.payer_id })
        </td>
    </tr>
}

</table>
