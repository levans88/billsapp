@model IEnumerable<billsapp.Models.payment_method>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.payment_method_name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.payment_method_abbreviation)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.payer.payer_property_01)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.status.status_description)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.payment_method_name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.payment_method_abbreviation)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.payer.payer_property_01)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.status.status_description)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.payment_method_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.payment_method_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.payment_method_id })
        </td>
    </tr>
}

</table>
